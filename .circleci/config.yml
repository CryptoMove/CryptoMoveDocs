version: 2
jobs:
  deploy:
    docker:
      - image: circleci/node:10-browsers
    steps:
      - checkout
      - run: yarn
      - run: yarn deploy
workflows:
  version: 2
  build-deploy:
    jobs:
      - deploy:
          filters:
            branches:
              only: master
